## MODIFIED Requirements

### Requirement: 오디오 장치 연결 상태 자동 감지 및 갱신
시스템은 오디오 장치가 새로 연결되거나 연결이 끊어졌을 때 이를 감지하여 장치 목록을 자동으로 갱신해야 한다. (수동 새로고침 버튼 미제공) 
이때, 시스템에서 인식된 이력이 있는 장치가 물리적으로 연결이 해제되거나 비활성화(Disabled) 되더라도 콤보박스 목록에서 완전히 제거하지 않고 계속 표시해야 한다. 단, 이러한 비활성화 또는 연결 해제 상태인 장치는 사용자가 시각적으로 인지할 수 있도록 목록에서 반투명하게(Opacity 적용 등) 표시되어야 한다. 
장치의 상태가 변경(연결 해제 -> 재연결 등)될 때마다 목록은 최신 상태로 정상 갱신되어야 하며, 연결이 복구되면 다시 원래의 불투명도로 돌아와야 한다.

#### Scenario: 장치 연결/해제 시 자동 갱신 및 시각적 피드백
- **WHEN** 새로운 USB 헤드셋이 시스템에 연결되거나 기존 장치가 해제될 때
- **THEN** 시스템은 즉시 이벤트를 감지하고, 수동 조작 없이 프로파일 내의 장치 선택 콤보박스 목록을 최신 상태로 자동 갱신해야 한다. 연결이 끊긴 장치는 목록에 남아있되 반투명하게 표시되어야 하며, 다시 연결될 경우 정상적인 불투명도로 갱신되어야 한다.

### Requirement: 선호 장치 순환 전환 (사용자 지정 순서)
전환 명령이 발생하면, 시스템은 사용자가 배치한 순서에 따라 다음 '프로파일'에 지정된 입출력 장치로 기본 장치를 변경해야 한다. 단, 전환 대상이 되는 프로파일에 설정된 장치 중 하나라도 연결이 끊어졌거나 사용할 수 없는 상태(비활성화 등)인 경우, 해당 프로파일로의 전환을 스킵하고 그 다음으로 설정된 유효한 프로파일을 찾아 전환해야 한다.

#### Scenario: 지정된 순서대로 프로파일 전환
- **WHEN** 사용자가 '헤드셋 프로파일 -> 스피커 프로파일' 순서로 배치한 상태에서 전환 명령이 발생할 때
- **THEN** 활성화 상태가 '헤드셋 프로파일'에서 '스피커 프로파일'로 넘어가고, 해당 프로파일에 설정된 입출력 장치가 모두 기본 장치로 변경되어야 한다

#### Scenario: 연결이 끊긴 장치가 포함된 프로파일 스킵
- **WHEN** 사용자가 '스피커 프로파일 -> 헤드셋 프로파일(연결 끊김) -> 이어폰 프로파일' 순서로 배치한 상태에서 스피커 프로파일 사용 중 전환 명령이 발생할 때
- **THEN** 헤드셋 프로파일의 장치가 사용할 수 없는 상태이므로 이를 스킵하고, 바로 '이어폰 프로파일'로 활성화 상태가 넘어가며 해당 장치로 전환되어야 한다
