## 1. 설정 모델 수정

- [x] 1.1 앱 설정 모델에 `ShowProfileChangeNotification` (bool) 속성 추가 및 기본값을 true로 설정
- [x] 1.2 설정 저장 및 로드 로직에 새 속성이 정상적으로 직렬화/역직렬화되는지 확인

## 2. 알림 로직 공통화

- [x] 2.1 기존 핫키 전환 시 사용되던 알림 표시 로직을 별도의 공통 메서드(예: `App.ShowNotification` 또는 `NotificationService`)로 분리 및 캡슐화
- [x] 2.2 공통 알림 메서드 내에 `ShowProfileChangeNotification` 설정 값을 확인하는 조건문 추가 (설정이 false면 즉시 반환)
- [x] 2.3 기존 핫키 처리부에서 공통 알림 메서드를 사용하도록 리팩토링

## 3. UI 연동

- [x] 3.1 `SoundSwitcher.xaml` 등 설정 메뉴 UI에 "프로파일 변경 알림 표시" 체크박스(토글) 추가 및 바인딩
- [x] 3.2 프로파일 카드를 클릭하여 전환하는 `MainViewModel` 내의 활성화 로직(`SetActiveProfile` 등) 직후 공통 알림 메서드 호출

## 4. 검증

- [x] 4.1 UI 메뉴에서 프로파일 카드 클릭 시 변경 알림이 정상적으로 표시되는지 테스트
- [x] 4.2 설정 메뉴에서 알림 설정을 끄고 핫키 및 클릭 전환을 수행할 때 알림이 뜨지 않는지 테스트
- [x] 4.3 앱 재시작 후 설정 값과 알림 표시 여부가 올바르게 유지되는지 테스트
