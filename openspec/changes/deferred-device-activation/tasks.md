## 1. 핵심 상태 및 이벤트 모니터링

- [ ] 1.1 지연된 장치 활성화 상태를 추적하기 위해 프로파일(Profile)/장치 모델 및 뷰모델에 `IsPending` 상태 추가.
- [ ] 1.2 `AudioDeviceManager` 등 핵심 클래스에 `IMMNotificationClient`(또는 기존 장치 상태 리스너)를 구현하여 장치 상태 변경 이벤트(예: `OnDeviceStateChanged`) 연결.
- [ ] 1.3 장치 상태 변경 이벤트가 메인 UI 및 전환 로직 스레드로 올바르게 전달되는지 확인.

## 2. 장치 전환 로직 업데이트

- [ ] 2.1 프로파일 전환 로직을 업데이트하여 실행 전 대상 장치의 사용 가능 여부 확인 기능 추가.
- [ ] 2.2 대상 장치가 연결 해제/비활성 상태인 경우 에러나 스킵 처리 대신 프로파일의 상태를 `Pending`으로 설정하고 대기 중인 요청으로 저장.
- [ ] 2.3 대상 장치의 상태가 활성화(Active)로 변경될 때 대기 중인 프로파일 전환을 자동으로 실행하는 핸들러 추가.
- [ ] 2.4 새로운 프로파일 전환이 요청되면 기존에 대기 중이던 전환 요청을 취소하는 로직 구현.
- [ ] 2.5 흩어져 있던 알림(Notification) 발생 코드를 제거하고, 핵심 전환 로직 내 실제 장치 전환 성공 시점에만 공통으로 트리거되도록 이동.

## 3. 메인 UI 프로파일 카드 및 트레이 툴팁 업데이트

- [ ] 3.1 `IsPending` 상태를 읽을 수 있도록 프로파일 카드의 데이터 템플릿(DataTemplate/ViewModel) 업데이트.
- [ ] 3.2 `IsPending`이 참(true)일 때 프로파일 아이콘 우측 하단의 기존 초록색 체크 표시(Checkmark)를 대체할 로딩 스피너 애니메이션 요소 추가.
- [ ] 3.3 로딩 스피너가 대기 상태를 정확히 반영하고 전환이 완료되거나 취소되면 즉시 체크 표시 또는 빈 상태로 돌아가는지 확인.
- [ ] 3.4 장치가 대기(Pending) 상태로 진입 시 트레이 아이콘의 이미지는 유지하되, 마우스 오버 툴팁(Tooltip) 텍스트에 대기 중인 프로파일 이름과 상태를 명시하도록 로직 추가.

## 4. 테스트 및 검증

- [ ] 4.1 메인 화면에서 연결이 끊긴 장치의 프로파일을 클릭할 때 즉시 실패하는 대신 아이콘에 정상적으로 스피너가 표시되는지 검증.
- [ ] 4.2 대기 중인 대상 장치의 전원을 켤 때 시스템이 자동으로 전환을 수행하고 프로파일 카드에 스피너 대신 초록색 체크가 표시되는지 검증.
- [ ] 4.3 대기 상태인 프로파일이 있을 때 다른 프로파일을 선택하면 첫 번째 대기 요청이 취소되는지 검증.
- [ ] 4.4 대기 기간 동안 불필요한 CPU 리소스 소모(폴링 루프 등)가 없는지 성능 프로파일러로 검증.
- [ ] 4.5 프로파일 전환 실패 시 또는 대기 진입 시 허위 알림이 뜨지 않고, 전환 성공 시점에만 알림이 뜨는지 검증.
