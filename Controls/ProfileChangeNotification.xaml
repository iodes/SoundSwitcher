<UserControl x:Class="SoundSwitcher.Controls.ProfileChangeNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300"
             Background="Transparent"
             Focusable="False"
             IsHitTestVisible="False">

    <!-- UI Root -->
    <Border x:Name="RootBorder" 
            Background="{DynamicResource ApplicationBackgroundBrush}" 
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1" 
            CornerRadius="8" 
            Padding="16,12" 
            Margin="10" 
            Opacity="0">
        <Border.RenderTransform>
            <TranslateTransform Y="0" />
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="4" Direction="270" Opacity="0.3" />
        </Border.Effect>
        <StackPanel>
            <TextBlock Text="장치 전환됨" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,6" />
            <TextBlock x:Name="MessageText" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
        </StackPanel>
    </Border>
</UserControl>
